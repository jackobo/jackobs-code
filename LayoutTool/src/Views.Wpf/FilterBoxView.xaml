<UserControl x:Class="LayoutTool.Views.Wpf.FilterBoxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LayoutTool.Views.Wpf"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             mc:Ignorable="d" 
             VerticalAlignment="Center"
             >
    <Border BorderBrush="Black" BorderThickness="3,0,0,0"  Padding="5,0,5,0" Cursor="SizeAll" Background="Transparent">
        <i:Interaction.Behaviors>
            <local:FrameworkElementDragBehavior/>
        </i:Interaction.Behaviors>
        <TextBlock Text="{Binding Name}" FontWeight="Bold" Background="Transparent" Margin="0,0,20,0"/>
    </Border>
</UserControl>
