<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:workspace="clr-namespace:GamesPortal.Client.ViewModels.Workspace;assembly=GamesPortal.Client.ViewModels">
    <DataTemplate DataType="{x:Type workspace:ExternalGamesWorkspaceItem}">
        <DataGrid ItemsSource="{Binding Games}" AutoGenerateColumns="True" SelectionUnit="Cell">
            <!--
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Name, Mode=OneWay}" Header="Name" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding GameTypes, Mode=OneWay}" Header="Game types" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding SupportedTechnologies, Mode=OneWay}" Header="Supported technologies" Width="Auto"/>
            </DataGrid.Columns>
            -->
        </DataGrid>
    </DataTemplate>
</ResourceDictionary>