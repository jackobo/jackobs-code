<UserControl x:Class="GGPMockBootstrapper.Views.AddNewUserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Column="0" Grid.Row="0" Margin="10,0,10,0" TextAlignment="Center">User Name:</TextBlock>
        <TextBlock Grid.Column="0" Grid.Row="1" Margin="10,0,10,0" TextAlignment="Center">CID:</TextBlock>
        <TextBox Grid.Column="1" Grid.Row="0" Width="200" Text="{Binding Path=UserName}"></TextBox>
        <TextBox Grid.Column="1" Grid.Row="1" Width="200" Text="{Binding Path=CID}"></TextBox>
    </Grid>
</UserControl>
