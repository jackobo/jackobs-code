<UserControl x:Class="GGPMockBootstrapper.Views.AddNewHtml5GameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="70"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Game type:" FontSize="12" Margin="2"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding GameType}" MinWidth="100" MaxWidth="120" HorizontalAlignment="Left" Margin="2"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Base URL:"  FontSize="12" Margin="2"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding BaseUrl}" MinWidth="400" Margin="2"/>

    </Grid>
</UserControl>
